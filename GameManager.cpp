#include "GameManager.h"

unsigned int GameManager::Play() {
    while( !GameManager::gameOver) {
    }
}

void GameManager::Turn( void ) {

    string input = "";
    cout << "Cast line? (y/n): ";
    cin >> input;
    cout << endl;
    
}